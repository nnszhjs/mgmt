# BPR_PC: BPR + Popularity Compensation
# Inherits all BPR parameters, adds pc_lambda.
embedding_size: 64     # (int) The embedding size of users and items.
pc_lambda: 1.0         # (float) Weight of log-popularity penalty at inference.
