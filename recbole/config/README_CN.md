# recbole.config

RecBole 的配置模块。提供 `Config` 类，用于从内置 YAML 属性文件加载默认参数，并与来自配置文件、命令行参数和参数字典的外部输入进行合并，优先级顺序为：命令行 > 参数字典 > 配置文件。

## 文件

| 文件 | 描述 |
|------|------|
| `__init__.py` | 包初始化文件；导出 `Config` 类。 |
| `configurator.py` | 实现 `Config` 类，负责参数加载、合并、验证和不同输入源之间的兼容性检查。自动配置评估设置、模型类型和设备分配。 |
